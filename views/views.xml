<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="pedido.list">
      <field name="name">pedido list</field>
      <field name="model">pedido.pedido</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="pedido.action_window">
      <field name="name">pedido window</field>
      <field name="res_model">pedido.pedido</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="pedido.action_server">
      <field name="name">pedido server</field>
      <field name="model_id" ref="model_pedido_pedido"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="pedido" id="pedido.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="pedido.menu_1" parent="pedido.menu_root"/>
    <menuitem name="Menu 2" id="pedido.menu_2" parent="pedido.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="pedido.menu_1_list" parent="pedido.menu_1"
              action="pedido.action_window"/>
    <menuitem name="Server to list" id="pedido" parent="pedido.menu_2"
              action="pedido.action_server"/>
    -->
  </data>
</odoo>